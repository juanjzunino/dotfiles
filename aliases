# Listing options
alias ls="ls -G"
alias l="ls -lafG"

# Sane defaults
alias cp="cp -i"
alias mv="mv -i"


# Send files to trash
alias ts="trash"

# Git
alias gs="git status"
alias gg="git graph"
alias gu="git pull"


# Jupyter Notebooks
alias jn="jupyter notebook"
